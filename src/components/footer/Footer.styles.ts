import styled from "styled-components";

export const FooterStyled = styled.footer`
  margin-top: auto;
  border-width: 0.1rem 0 0;
  border-style: solid;
  border-color: black;
  text-align: center;
  padding: 1.6rem 0;

  & > p {
    font-size: 1.3rem;
    font-style: italic;
  }
`;
